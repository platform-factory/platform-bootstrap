# Usually nothing to put here. project_id defaults to the same value as
# 0-foundation ("platform-factory-ref") — copy to terraform.tfvars ONLY if
# you overrode project_id in 0-foundation's tfvars, and set the same value
# here so this layer can find 1-cluster's state bucket.

# project_id = "platform-factory-ref"
